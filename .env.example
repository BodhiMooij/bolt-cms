# Database (PostgreSQL – e.g. Supabase)
# Supabase: Project Settings → Database → Connection string.
# IMPORTANT: Use the "Transaction" pooler (port 6543), NOT the "Direct" one (db.xxx.supabase.co).
# On live: set DATABASE_URL in your host's env (e.g. Vercel → Settings → Environment Variables).
# Use the raw URI with no extra quotes or spaces; special chars in password must be URL-encoded.
# Format: postgresql://postgres.[project-ref]:[YOUR-PASSWORD]@aws-0-[region].pooler.supabase.com:6543/postgres?pgbouncer=true
DATABASE_URL="postgresql://postgres.[project-ref]:[YOUR-PASSWORD]@aws-0-[region].pooler.supabase.com:6543/postgres?pgbouncer=true"

# Auth (NextAuth.js) – required for GitHub login
AUTH_SECRET="generate-with-openssl-rand-base64-32"
AUTH_GITHUB_ID="your-github-oauth-app-client-id"
AUTH_GITHUB_SECRET="your-github-oauth-app-client-secret"
